node_modules/
.DS_Store
.worktrees/
.claude/settings.local.json

# Local env files with real secrets — never commit
api/.env
frontend/.env.local

# Frontend build output
frontend/dist/

# PM2 logs
*.log

# Lead data — never commit
api/leads.ndjson
*.ndjson

# Founder memory kernel — canonical store lives outside the repo.
# Anchored to repo root: ignore a top-level/api memory STORE dir, but NOT nested source dirs
# like api/src/memory or api/tests/memory (the memory-v2 engine code).
/memory/
/api/memory/

# Local design/superpowers scratch.
.superpowers/
docs/superpowers/
_staging*/
.superpowers/

# Cloudflare Wrangler build cache
.wrangler/
